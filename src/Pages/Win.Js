const Win = ({
    changePage,
    currentPlayer,
    setCurrentPlayer,
    score,
    bossChoice,
    setBossChoice,
    setScore,
    specialAttackCount,
    setSpecialAttack,
  }) => {
    return (
       <div className="Win_Page">
           <h1>You Have Cleared The Krypt</h1>
       </div>
    )
}

export default Win